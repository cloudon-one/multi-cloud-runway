# Placeholder Register (G1-5)

Generated by `scripts/placeholder-scan.py` — do not hand-edit; regenerate
with `make placeholders`. `make placeholder-gate` fails while any token
remains (pre-apply gate, GR-4).

**9 distinct tokens, 24 occurrences.**

| Token | Occurrences |
|---|---|
| `PLACEHOLDER_ADMIN_GROUP_EMAIL` | `.github/gate-thresholds.yaml:29`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:43` |
| `PLACEHOLDER_CIDR_INSPECTION_EU` | `.github/gate-thresholds.yaml:24`, `aws-terragrunt-configuration/aws/vars.yaml:740` |
| `PLACEHOLDER_CIDR_INSPECTION_US` | `.github/gate-thresholds.yaml:23`, `aws-terragrunt-configuration/aws/vars.yaml:503` |
| `PLACEHOLDER_CI_SA_EMAIL` | `.github/gate-thresholds.yaml:30`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:45`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:51`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:58` |
| `PLACEHOLDER_CORP_EGRESS_CIDR` | `.github/gate-thresholds.yaml:28`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:41` |
| `PLACEHOLDER_GCP_DIRECTORY_CUSTOMER_ID` | `.github/gate-thresholds.yaml:25`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:79` |
| `PLACEHOLDER_OU_ID_STG` | `.github/gate-thresholds.yaml:22`, `aws-terragrunt-configuration/aws/vars.yaml:243`, `aws-terragrunt-configuration/aws/vars.yaml:248`, `aws-terragrunt-configuration/aws/vars.yaml:253`, `aws-terragrunt-configuration/aws/vars.yaml:258`, `aws-terragrunt-configuration/aws/vars.yaml:263` |
| `PLACEHOLDER_PROJECT_NUMBER_STG_HOST` | `.github/gate-thresholds.yaml:26`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:37` |
| `PLACEHOLDER_PROJECT_NUMBER_STG_SERVICE` | `.github/gate-thresholds.yaml:27`, `gcp-terragrunt-configuration/terragrunt/vars.yaml:38` |

## How to resolve

### ADMIN

Resolve with the owning team; the token name encodes the value type. Never replace with an invented value (GR-4).

### CIDR

Resolve from the corporate IPAM / network team. Must not overlap any range in NETWORK_TOPOLOGY.md; run `make score` (IP Planning) after filling.

### CI

Resolve with the owning team; the token name encodes the value type. Never replace with an invented value (GR-4).

### CORP

Resolve from the corporate egress IP allocation (network team). Used for VPC-SC access levels and firewall allowlists.

### GCP

Resolve with the owning team; the token name encodes the value type. Never replace with an invented value (GR-4).

### OU

Resolve with the owning team; the token name encodes the value type. Never replace with an invented value (GR-4).

### PROJECT

Resolve from `gcloud projects describe <project-id> --format='value(projectNumber)'` after the project exists; project numbers cannot be known at plan time (see CMEK_WIRING.md workflow).

