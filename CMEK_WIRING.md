# CMEK Wiring (G4-11)

Post-apply operator commands: grant each GCP service agent
`roles/cloudkms.cryptoKeyEncrypterDecrypter` on its service key.
Generated by `scripts/render-cmek-wiring.py` from `vars.yaml` — do not
hand-edit; regenerate with `make cmek-wiring`.

Service agents embed the **project number**, which exists only after
the project is created — that is why these grants cannot be fully
declared at plan time. For each project below, first resolve:

```
PROJECT_NUMBER=$(gcloud projects describe <PROJECT_ID> --format='value(projectNumber)')
```

## stg/eu

### Project: `cloudon-stg-eu-host` (host)

```bash
PROJECT_NUMBER=$(gcloud projects describe cloudon-stg-eu-host --format='value(projectNumber)')
gcloud kms keys add-iam-policy-binding gke \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@container-engine-robot.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding sql \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gcp-sa-cloud-sql.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding storage \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gs-project-accounts.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding redis \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@cloud-redis.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding logging \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gcp-sa-logging.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding pubsub \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gcp-sa-pubsub.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
```

### Project: `cloudon-stg-eu-svc-service` (service/service)

```bash
PROJECT_NUMBER=$(gcloud projects describe cloudon-stg-eu-svc-service --format='value(projectNumber)')
gcloud kms keys add-iam-policy-binding gke \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@container-engine-robot.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding sql \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gcp-sa-cloud-sql.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding storage \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gs-project-accounts.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding redis \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@cloud-redis.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding logging \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gcp-sa-logging.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
gcloud kms keys add-iam-policy-binding pubsub \
  --keyring cloudon-stg-eu-europe-west1 --location europe-west1 \
  --project cloudon-stg-eu-host \
  --member 'serviceAccount:service-${PROJECT_NUMBER}@gcp-sa-pubsub.iam.gserviceaccount.com' \
  --role roles/cloudkms.cryptoKeyEncrypterDecrypter
```

<!-- generated-from: sha256:584486baa63afe3b7c8a7e511a42461a55bfc3ae34216f146e594f4cea1af1c1 -->

