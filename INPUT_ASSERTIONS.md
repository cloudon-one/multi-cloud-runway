# Input Assertions (G1-7)

Declared-but-not-emitted findings: keys present in a vars.yaml that no
terragrunt stack or module consumes. Generated by
`scripts/input-assertions.py`; regenerate with `make assertions`.

**0 active findings, 16 allowlisted.** AWS `inputs.*` keys are UNVERIFIED at key level (external Git modules; L2 applies to GCP local modules only).

## Allowlisted (with reasons)

| Key | Reason |
|---|---|
| `Environments.us-dev.Resources.tgw` | Vestigial per-env TGW block; the global network/tgw stack reads Environments.network only (its routes are hardcoded in its hcl). Removing the block is a functional vars change deferred to the G3-4 TGW route-domain work, where TGW config becomes env-driven. |
| `envs.shrd.dev.resources.net-peering.inputs.billing_account` | Injected by the shared YAML anchor merge used across resource blocks; the peering module declares no such variable. Inert by construction. |
| `envs.shrd.dev.resources.net-peering.inputs.domain` | Same anchor-merge artifact as billing_account; inert by construction. |
| `envs.shrd.dev.resources.svc-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.shrd.prod.resources.net-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.shrd.prod.resources.net-peering.inputs.billing_account` | Same anchor-merge artifact; prod tree is read-only in this plan (GR-3). |
| `envs.shrd.prod.resources.net-peering.inputs.domain` | Same anchor-merge artifact; prod tree is read-only in this plan (GR-3). |
| `envs.shrd.prod.resources.svc-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.dev.eu.resources.svc-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.stg.eu.resources.net-glb` | GLB declared for stg/eu but no stack exists (prod has one). Creating the stg GLB stack is a G4 scope decision; recorded, not hidden. |
| `envs.stg.eu.resources.net-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.stg.eu.resources.svc-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.prod.us.resources.svc-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.prod.us.resources.svc-gke.inputs.enable_network_egress_expoert` | Typo of enable_network_egress_export in PROD vars — the input is silently ignored, so network egress export is effectively OFF in prod GKE. Prod is read-only in this plan (GR-3); fix logged in docs/preprod/DEFERRED_PROD_CHANGES.md. |
| `envs.prod.eu.resources.svc-iam-permissions` | Pre-existing declared-but-undeployed IAM permission block recorded at G1 baseline. Deploying the stack or deleting the block is a scope decision for G4 (stg) / DEFERRED_PROD_CHANGES.md (prod, GR-3); the evidence-spine gate makes no functional changes. |
| `envs.prod.eu.resources.svc-gke.inputs.enable_network_egress_expoert` | Typo of enable_network_egress_export in PROD vars — the input is silently ignored, so network egress export is effectively OFF in prod GKE. Prod is read-only in this plan (GR-3); fix logged in docs/preprod/DEFERRED_PROD_CHANGES.md. |

